// Engine unit tests live under test/engine/. The default widget smoke test is
// omitted because the app requires media_kit native libraries that are not
// available in the host test environment.

void main() {}
